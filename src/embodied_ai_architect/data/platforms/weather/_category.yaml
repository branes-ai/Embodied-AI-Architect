# Category: weather
# Auto-generated index — shared defaults for all weather platforms

category: weather
description: Shared defaults for 3 weather platform(s)
platform_count: 3
default_attributes:
  power_watts:
    min: 500
    max: 20000
    typical: 5000
  weight_kg:
    min: 1
    max: 1000
    typical: 50
  cost_usd:
    min: 1000
    max: 500000
    typical: 50000
  latency_ms:
    min: 10
    max: 500
    typical: 50
common_classification:
  locomotion: ground.wheeled
  environment:
  - outdoor_extreme
typical_constraints:
  max_power_watts: 5000
  max_latency_ms: 50
  max_cost_usd: 50000

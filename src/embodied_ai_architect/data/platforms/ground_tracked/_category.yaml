# Category: ground_tracked
# Auto-generated index — shared defaults for all ground_tracked platforms

category: ground_tracked
description: Shared defaults for 6 ground tracked platform(s)
platform_count: 6
default_attributes:
  power_watts:
    min: 50
    max: 5000
    typical: 500
  weight_kg:
    min: 1
    max: 1000
    typical: 50
  cost_usd:
    min: 1000
    max: 500000
    typical: 50000
  latency_ms:
    min: 10
    max: 500
    typical: 50
common_classification:
  locomotion: ground.tracked
  environment:
  - hazardous
  - outdoor_extreme
typical_constraints:
  max_power_watts: 500
  max_latency_ms: 50
  max_cost_usd: 50000
